= provide(:title, 'Reseptit')

%h1 Reseptit

.container
  .span6.offset3
    = render "shared/nav"

.span6.offset3
  = will_paginate

.container
  .span6.offset3.well
    %table.table.table-striped
      %thead
        %th Nimi
        %th Hinta
      %tbody
        - @recipes.each do |recipe|
          %tr
            %td= link_to recipe.name, recipe
            %td 
              = recipe.get_price.round(2)
              €
.container
  .span6.offset3
    = link_to :controller => "recipes", :action => "new", :bakery_id => @bakery.id do
      %i.icon-plus
      Lisää resepti

  .span6.offset3
    = will_paginate

