- provide(:title, 'Luo resepti')
  
%h1 Luo resepti
.row
  .span6.offset3
    = form_for @recipe do |f|
      = render 'shared/recipe_error_messages'
      
      %p
        = f.label :name, "Nimi" 
        = f.text_field :name 
        
        = hidden_field_tag :firm_id, @firm.id
        
        %h4 Raaka-aineet:
        
        %p#recipe_materials
        
      %p.well  
        = select("post", "material_id", @firm.materials.collect {|m| [ m.name, m.id ] }, {:prompt => 'Valitse raaka-aine'})
        = fields_for :material do |h|
          = h.label :amount, "Määrä"
          = h.number_field :amount, min: 0
        
        = button_to_function "Lisää", "add_material(event)", :class => "btn btn-mini"
        
        
      = f.submit "Luo resepti", class: "btn btn-large btn-primary"
    
    = link_to request.env["HTTP_REFERER"] do
      %i.icon-arrow-left
      Takaisin
