= provide(:title, 'Asiakkaat')

%h1 Asiakkaat

.container
  .span8.offset2
    = render "shared/nav"

= will_paginate

.container
  .span8.offset2.bordered
    %table.table
      %thead
        %th
        %th Nimi
        %th Puhelinnumero
        %th Osoite
        %th Kaupunki
        %th Muokkaa
        
      %tbody
        - @clients.each do |client|
          %tr
            %td= link_to "Näytä", client
            %td= client.name
            %td= client.phone
            %td= client.address
            %td= client.city
            %td
              = link_to :controller => "clients", :action => "edit", :id => client.id do
                %i.icon-edit

= will_paginate

.container
  .span8.offset2
    = link_to :controller => "clients", :action => "new", :firm_id => @firm.id do
      %i.icon-plus
      Lisää asiakas

