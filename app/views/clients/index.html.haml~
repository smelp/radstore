= provide(:title, 'Asiakkaat')

%h1 Asiakkaat

= will_paginate

.container
  .span6.offset3
    %table.table
      %thead
        %th Nimi
        %th Osoite
        %th Kaupunki
        %th Puhelinnumero
        
      %tbody
        - @clients.each do |client|
          %tr
            %td= link_to client.name, client
            %td= client.address
            %td= client.city
            %td= client.phone

= will_paginate

.container
  .span6.offset3
    = link_to :controller => "clients", :action => "new", :firm_id => @firm.id do
      %i.icon-plus
      Lisää asiakas

