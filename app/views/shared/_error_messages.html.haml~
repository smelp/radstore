- if @user.errors.any?
  #error_explanation
    .alert.alert-error
      Lomake sisältää #{@user.errors.count} virhettä
    %ul
      - @user.errors.each_with_index do |msg, i|
        %li* #{msg[i+1]}
