- if @firm.errors.any?
  #error_explanation
    .alert.alert-error
      Lomake sisältää #{@firm.errors.count} virhettä
    %ul
      - @firm.errors.each_with_index do |msg, i|
        %li* #{msg[i+1]}
