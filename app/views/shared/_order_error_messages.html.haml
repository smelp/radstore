- if @order.errors.any?
  #error_explanation
    .alert.alert-error
      Lomake sisältää #{@order.errors.count} virhettä
    %ul
      - @order.errors.each_with_index do |msg|
        %li* #{msg[1]}
