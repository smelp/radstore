= provide(:title, 'Yritykset')
%h1 Yrityksesi

.container.bordered

  = will_paginate
  - @static_pages.each do |firm|
    = render firm
    
= will_paginate

.container
  - if is_admin?
    = link_to new_firm_path do
      %i.icon-plus
      Lisää yritys
