= provide(:title, 'Käyttäjät')
%h1 Kaikki käyttäjät

.container.bordered
  .span7.offset1
    .span7.offset1
      = will_paginate
    .well.span7.offset1
      %ul.users
        - @users.each do |user|
          %li= render user
      = will_paginate
